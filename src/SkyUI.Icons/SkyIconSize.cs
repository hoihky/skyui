namespace SkyUI.Icons;

/// <summary>Standard Sky icon sizes (px) for buttons, navigation, and list rows.</summary>
public enum SkyIconSize
{
    /// <summary>16px — compact buttons and dense UI.</summary>
    Small = 16,

    /// <summary>20px — navigation and list items.</summary>
    Medium = 20,

    /// <summary>24px — prominent actions and section headers.</summary>
    Large = 24,
}
